spring:
  datasource:
    url: jdbc:h2:mem:iching;MODE=MySQL;DB_CLOSE_DELAY=-1;DATABASE_TO_LOWER=TRUE
    username: sa
    password:
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: none
  flyway:
    enabled: true

iching:
  jwt:
    secret: test-jwt-secret-key-iching-mvp-32b
    ttl-seconds: 3600
  aes:
    secret: test-aes-secret16
